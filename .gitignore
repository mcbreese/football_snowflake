
.databricks
logs/
# Ignore VS Code workspace settings containing secrets
.vscode/
/.vscode
.vscode/settings.json
profiling/data
rsa_key.p8
rsa_key.pub

# Claude Code's own read-only Snowflake service credentials - separate
# identity from the personal login above, see snowflake/setup_claude_readonly_role.sql.
# Also the shared local credential for anyone's SQLFluff/ad hoc dbt use -
# see secrets/.env.readonly and CLAUDE.md's Auth section.
secrets/