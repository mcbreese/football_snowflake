
logs/
# uv-managed virtualenv - .venv/pyvenv.cfg was tracked by accident since
# the very first commit (it embeds a machine-specific absolute path plus
# the local Python version, neither of which belong in the repo).
.venv/
# Ignore VS Code workspace settings containing secrets
.vscode/
/.vscode
.vscode/settings.json
profiling/data
rsa_key.p8
rsa_key.pub

# Claude Code's own read-only Snowflake service credentials - separate
# identity from the personal login above, see snowflake/setup_claude_readonly_role.sql.
# Also the shared local credential for anyone's SQLFluff/ad hoc dbt use -
# see secrets/.env.readonly and CLAUDE.md's Auth section.
secrets/

# Claude Code's own local project state - permission grants
# (settings.local.json) and worktree checkouts (worktrees/, which can
# contain nested .git dirs and shouldn't ever be tracked). Nothing in
# here is meant to be shared or committed.
.claude/